# lattice_async.py settings file
# Format: KEY=VALUE (same style as .env files). Blank lines and # comments are ignored.
#
# DEFAULTS (if a key is omitted):
# - *_MAX_IN_FLIGHT: 2
# - *_MIN_INTERVAL_SECONDS: 0.0
# - *_MAX_RETRIES: 6
# - *_BACKOFF_BASE_SECONDS: 1.0
# - *_BACKOFF_MAX_SECONDS: 30.0
# - *_BACKOFF_JITTER: 0.2
#
# Notes:
# - Existing process env vars take precedence over this file.
# - This file provides conservative settings to reduce 429s.
# - For OpenAI Tier-2, these are tuned for the openai-none lattice bottleneck model (gpt-4.1).
#
# Recommended CLI:
# - openai-none: python3 lattice_async.py --lattice openai-none --max-concurrent-games 4
# - mixed-low:   python3 lattice_async.py --lattice mixed-low   --max-concurrent-games 3

# ---------- OpenAI (Tier-2 preset for openai-none) ----------
OPENAI_MAX_IN_FLIGHT=2
OPENAI_MIN_INTERVAL_SECONDS=0.25
OPENAI_MAX_RETRIES=8
OPENAI_BACKOFF_BASE_SECONDS=1
OPENAI_BACKOFF_MAX_SECONDS=60
OPENAI_BACKOFF_JITTER=0.25

# ---------- Mixed-low provider presets ----------
# OpenAI (gpt-5.4 in mixed-low) still uses your OpenAI tier caps.
# Keep the Tier-2 OpenAI values above.

# Anthropic: conservative starting point
ANTHROPIC_MAX_IN_FLIGHT=1
ANTHROPIC_MIN_INTERVAL_SECONDS=0.75
ANTHROPIC_MAX_RETRIES=8
ANTHROPIC_BACKOFF_BASE_SECONDS=1
ANTHROPIC_BACKOFF_MAX_SECONDS=60
ANTHROPIC_BACKOFF_JITTER=0.25

# Gemini: conservative starting point
GEMINI_MAX_IN_FLIGHT=1
GEMINI_MIN_INTERVAL_SECONDS=0.60
GEMINI_MAX_RETRIES=8
GEMINI_BACKOFF_BASE_SECONDS=1
GEMINI_BACKOFF_MAX_SECONDS=60
GEMINI_BACKOFF_JITTER=0.25

# ---------- Optional global fallbacks ----------
# These apply only for providers without a provider-specific value.
# LLM_MAX_IN_FLIGHT=2
# LLM_MIN_INTERVAL_SECONDS=0.0
# LLM_MAX_RETRIES=6
# LLM_BACKOFF_BASE_SECONDS=1.0
# LLM_BACKOFF_MAX_SECONDS=30.0
# LLM_BACKOFF_JITTER=0.2
